kind: Profile
schema_version: 1
id: room.living-room
layer: room
resources:
  - id: skin.playlist.new-shows
    type: KodiSmartPlaylist
    management: enforce
    desired: present
    selectors:
      - selector.baseline
      - selector.skin
      - selector.room
    requires: []
    intent:
      playlist:
        limit: 40
