# Domain Docs

Before exploring, read root `CONTEXT.md` when present and relevant ADRs under `docs/adr/`. If these files do not exist, proceed silently; the domain-modeling skill creates them lazily.

## File structure

This is a single-context repo:

/
├── CONTEXT.md
├── docs/adr/
└── src/

Use glossary vocabulary from `CONTEXT.md` in issues, proposals, hypotheses, and tests. If output contradicts an ADR, surface the conflict explicitly rather than silently overriding it.
