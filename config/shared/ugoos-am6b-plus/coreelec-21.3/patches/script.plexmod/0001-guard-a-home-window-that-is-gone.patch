# script.plexmod 1.3.19
#
# On `System.OnQuit` the home window may already be gone, and dereferencing it
# raises inside the notification handler while Kodi is shutting down.
--- a/lib/monitor.py
+++ b/lib/monitor.py
@@ -106,8 +106,9 @@
             self.trigger('system.exit')
             self.actionStop()
             LOG("OnQuit: Closing Home")
-            windowutils.HOME.closeOption = "kodi_exit"
-            windowutils.HOME.doClose()
+            if windowutils.HOME:
+                windowutils.HOME.closeOption = "kodi_exit"
+                windowutils.HOME.doClose()
             return
 
     def stopPlayback(self):
