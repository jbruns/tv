# Master devices

Add one Markdown guide per installed device and link it from the
[Master room overview](../README.md). Shared setup belongs under
[`docs/`](../../../docs/).

Each device guide should contain:

```markdown
## Connections
## Device settings
## Network placement
## Home Assistant integration
```
